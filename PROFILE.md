# ImageGen

AI image generation API by ForgeMesh Labs / CoinopAI offering text-to-image, background removal, HD upscaling, and identity-preserving image transforms via FLUX models. Access is metered through x402 micropayments in USDC on Base mainnet, with no API key or signup.

- **Provider:** https://imagegen.coinopai.com
- **Documentation:** https://imagegen.coinopai.com
- **Base URL:** https://imagegen.coinopai.com
- **Tags:** ai, image-generation, text-to-image, generative-ai, background-removal, image-upscaling, image-transform, x402, crypto-payments, usdc, base-mainnet, agent-native, mcp, pay-per-use
- **Public API:** yes
- **Agent-native (MCP / llms.txt / skills):** yes
- **Confidence:** 95/100

## APIs

- **AI Image Generation** — Text-to-image and image-transform REST API (HTTP GET routes) priced per-call via x402 USDC micropayments on Base mainnet. Includes standard/clean/HD/pro generation, image transform, presets, and free job-status polling. (`https://imagegen.coinopai.com`)

> REST API is publicly reachable but every substantive call requires an x402 USDC micropayment on Base mainnet (no API key/signup). OpenAPI 3.1.0 contract and llms.txt confirmed live. MCP server exists as the npm package 'forgemesh-imagegen' (v1.0.2) run LOCALLY via stdio (npx -y forgemesh-imagegen with WALLET_PRIVATE_KEY env var) — it is not a hosted HTTP MCP endpoint, so it is not cataloged as an MCPServer property. No AsyncAPI/GraphQL/Postman artifacts found. npm registry page could not be independently confirmed.

_Generated by apis.io from a community submission._